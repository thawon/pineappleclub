'use strict';

describe("authentication", function () {
    
    beforeEach(function () {
        
    });

    it('should override services via mock modules', function () {        
                        
        browser.get('/contact');
                
        browser.sleep(2000);
        
    });

});